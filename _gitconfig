[user]
	name = Lukas Anzinger
	email = lukas@lukasanzinger.at

[color]
	# Colorize output whenever possible (`diff`, `log`, etc. benefit from that).
	ui = auto

[alias]
	b = branch
	ca = commit -a
	ci = commit
	co = checkout
	m = checkout master
	s = status --short
	tagme = !sh -c '[ -z "$1" ] && exit 1 || git tag -s \"$1\" -m \"Release \"$1\"\"' sh
	tush = push --tags

# vim:ft=gitconfig
